{
  "private": true,
  "workspaces": {
    "packages": [
      "addon",
      "examples/*"
    ]
  },
  "scripts": {
    "format": "prettier --write '**/*.{js,json,md,yml}'"
  },
  "devDependencies": {
    "eslint": "^7.6.0",
    "eslint-plugin-react": "^7.20.5",
    "prettier": "^2.0.5"
  }
}
